/**
 * Default backend base URL for the Basketball Shot Form Analyzer API.
 * Override via Settings screen (persisted in AsyncStorage).
 * Use your machine's LAN IP when testing on a physical device (e.g. http://192.168.1.100:8000).
 */
export const DEFAULT_BACKEND_BASE_URL = 'http://localhost:8000';
